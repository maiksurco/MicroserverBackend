server:
  port: 7071
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/maiksurco/MicroserverBackend.git
          #username:
          #password:
          search-paths: config-data
          default-label: main
  security:
    user:
      name: root
      password: s3cre3t
